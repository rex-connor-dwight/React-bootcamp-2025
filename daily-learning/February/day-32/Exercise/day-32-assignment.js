const exponentialValue = Math.exp(2);
const logarithmicValue = Math.log(2);

// Classwork: 
    // 1 Find the value of the expression above
    // 2 convert the floating point values of the result from the expression into a whole number 

// Solution:

// console.log(`${exponentialValue} \n ${logarithmicValue}`)
console.log(Math.round(exponentialValue))
console.log(Math.round(logarithmicValue))